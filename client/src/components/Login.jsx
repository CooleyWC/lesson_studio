import React from 'react';
import { Container, Typography } from '@mui/material';

function Login() {
    return (
        <Container>
            <Typography sx={{paddingTop: '100px', fontSize: '200px'}}>
                Login
            </Typography>
        </Container>
    );
}

export default Login;